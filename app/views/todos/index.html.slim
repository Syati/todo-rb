h1 Todos
table
  thead
    tr
      th title
      th status
      th

  tbody
    - @todos.each do |todo|
      tr
        td = todo.title
        td = todo.status
        td
          = link_to 'edit', edit_todo_path(todo)